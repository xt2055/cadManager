# 零件视图一期：实施入口

编写日期：2026-09-09。

本目录包含零件视图一期的实施契约、执行进度和验证记录。实现状态以 [progress.md](./progress.md) 为准。

## 给实施模型的任务

请先完整阅读 [implementation.md](./implementation.md)，再按其中第 15 节的步骤顺序实施。

必须遵守：

1. 阅读仓库根目录 `AGENTS.md`，禁止启动子代理。
2. 先执行 `git status --short`，保留已有未提交修改；不要清理不认识的文件。
3. 实施本目录规定的一期范围，不自行扩展 PDM、版本审批、相似度算法或项目 ACL。
4. 复用已有前端标题栏提取，不重写 CAD 引擎，不把后端文件名识别接口当作标题栏识别来源。
5. 每完成一个步骤，运行该步骤的验证，再进入下一步。
6. 持续更新本目录下你新建的 `progress.md`，记录完成项、修改文件、命令结果和剩余问题。
7. 如果代码与本文基线不同，先记录真实差异；不得为了套用文档而删除后续代码。
8. 不能用假接口、固定示例数据或吞掉异常来通过验收。
9. 数据库集成测试未执行时必须写“未执行”，不能写“全部通过”。
10. 只在隔离测试 schema 中创建或清理测试数据，不操作真实业务数据。

## 本次最重要的约束

- 一个零件索引公开地址对应一个附件，数据库记录按附件版本区分。
- 同一附件可以关联多个项目，不能复制索引来表达借用关系。
- 项目显示编号使用现有 `drawings.project`；内部关联及筛选使用 `drawings.id`。
- 标题栏图号、总图图号、项目编号是三个不同字段。
- 人工修正保存在索引层，不改 CAD 文件、不改正式零件版本数据。
- 所有当前查询必须匹配 `attachments.current_version_id`。
- 缺少当前版本识别结果时显示待提取，不能拿旧版本字段顶替。
- 当前系统代码的读取策略是登录后共享读取；本文一期继承该策略，**不宣称已经实现按项目成员隔离**。具体边界见主文档第 5 节。

## 已安装系统升级

正常后端启动时，`data.EnsurePartIndexSchema` 在开放 HTTP 服务前执行内嵌的
`000034_attachment_title_blocks.sql` 和 `000035_part_indexes.sql`，并写入
`schema_migrations`。已执行的迁移跳过；升级与记录处于同一事务，使用事务锁
防止多个后端同时升级。失败时停止启动并输出具体错误。

这里仅自动执行这两项增量迁移，不重放历史数据库重建脚本。数据库账号需具备
相应建表、修改表权限；更新部署时必须重启新后端程序。

2026-09-09 曾复现：已安装数据库缺少 `attachment_title_blocks.revision` 和
`part_indexes`，导致标题栏读取及零件列表/筛选全部返回 500。已补齐当前数据库，
并用隔离 schema 验证旧标题栏结构升级、重复启动、快照读写及索引同步。

## 可直接发送给实施模型的指令

> 请实现 `docs/part-index/implementation.md` 定义的零件视图一期。
> 先读根目录 AGENTS.md 和本目录 README.md，再完整阅读 implementation.md。
> 按第 15 节逐步执行，每步实现后验证，并维护 progress.md。
> 接口、状态、版本规则、字段映射和错误处理以文档为准，不自行改名或省略。
> 复用已有前端标题栏提取与 CAD 预览。不要启动子代理。
> 完成后按第 16 节提交验证结果；未执行的测试明确列出。
