# 项目级指令与规范

## 禁用子代理
严禁使用 `task` 工具启动子代理（例如 `explore`、`general` 等）。
原因：子代理会开辟新会话，破坏提示词缓存（Prompt Cache）。所有探索、搜索、代码修改与执行操作均必须在主上下文中直接完成。

## 提交信息使用中文
所有 `git commit` 信息必须使用简体中文，标题与正文皆然。
沿用既有格式：`type(scope): 中文标题`，正文用中文分点说明改动与验证结果。
