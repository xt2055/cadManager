# 空白图纸模板

新建图纸使用 `blank.exb`，直接复制自用户在 CAXA 中创建的 `go/工程图文档2.exb`，不修改其内容。

新建流程以弹窗填写的名称和图号创建草稿零件并上传 EXB。现有附件和本地编辑流程保留原始 EXB，生成当前 DWG，交给本机 CAXA 编辑；结束编辑沿用已有回写和版本管理规则。

此前手工生成的 `blank.dxf` / `blank.dwg` 不再用于新建图纸。
