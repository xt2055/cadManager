# 可选：api 或 json。连接本机 Go 后端时使用 api。
VITE_DATA_PROVIDER=api

# 本机 Go 后端地址。生产环境可改为正式 API 地址，或使用相对路径配合反向代理。
VITE_API_BASE_URL=http://127.0.0.1:8080/api

# 客户端更新检查复用 VITE_API_BASE_URL，接口为 GET /updates/latest。
