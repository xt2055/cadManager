# 环境分离（Vite 约定）：
#   npm run dev / tauri dev  -> 读取 .env.development
#   tauri build / vite build -> 读取 .env.production
# .env 为两环境公共配置。本机需自建 .env.development 与 .env.production（不入 git）。
#
# .env.development 示例（本机开发）：
#   VITE_API_BASE_URL=http://localhost:8080/api
#
# .env.production 示例（客户端打包写入服务器真实内网 IP；服务器 IP 变更后改此文件重打客户端）：
#   VITE_API_BASE_URL=http://192.168.101.117:8080/api
#
# 浏览器直接访问后端伺服页面时走同源相对路径 /api，无需配置。

# 客户端更新检查复用 VITE_API_BASE_URL，接口为 GET /updates/latest；
# 也可在「系统设置 → 软件更新」中临时输入服务器地址，优先级高于此配置。
