# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

- **设计／技术人员**（角色 `designer`）。日常在办公室电脑前（大屏 + 鼠标键盘）工作。任务：导入老图并保留原图号与历史文件；维护图纸结构、零件、BOM 与借用关系；从生产反馈或甲方材料发起变更工单，逐项写清修改明细并上传成果；把成果送审。
- **审核／质量人员**（角色 `reviewer`）。任务：处理分派到自己的待审任务，看到准确的前后版本与证据，多轮退回与重提，签署结论。
- **系统管理员**（角色 `admin`）。任务：账号与角色、审核流配置、图纸资产、变更审批与记录、属性字典、转换队列、通知、操作与系统日志、客户端更新。
- 查图人员（车间、生产、管理）凭原图号定位正式图纸并查看发放与回复记录，使用环境同样是办公室电脑。
- 一人可兼任多个角色；角色先按以上三种落地，后续按实际需要增量增加（本次确认）。

## Product Purpose

企业内部一套部署的 CAD 图纸管理系统，管住"图纸的身份、版本、变更原因和正式用图"，让老图纸带原图号、原版本、原历史关系平稳进入系统，而不是重建一套编号。

成功意味着：任何有权限的人打开一个版本，都能看到**为什么改、改了什么、依据是什么、谁编制谁审核、发给了谁、对方回复了什么**；项目可以归集多张原有图号，每次正式改动都有原因、证据、修改明细、固定成果、完整轮次审核记录和明确的图纸／项目影响范围。

## Positioning

**原图号是图纸的稳定主身份**，项目与图纸是多对多关联而非复制；外部计划号、交货序号、产品名称、规格型号、甲方编号最多作为来源字段，绝不参与图纸唯一性、审核、发布或归档。这套取舍让本系统能承接企业十几年积累的老图台账，而任何以"计划号"或规格型号为主键的同类工具都会在导入第一步就要求用户改号。

配套的差异点是变更闭环的粒度：一项原因对应一张变更事项，内部拆多个执行任务，逐项修改说明、改前／改后固定原件、每一轮提交与退回、发放清单与回复分别留存，且退回和取消轮次引用的文件同样受保护——审计只查"是否正式版"的做法在这里被明确否决。

## Operating Context

- 公司内网单组织部署：Go 服务（默认 `:8080`）+ PostgreSQL，前端 Vue 3 桌面 Web 应用，另打包 Tauri 桌面壳。
- 服务器与 SMB 共享目录必须位于同一台机器（`CAD_SMB_SHARE`，开发默认 `CadWorking`），Go 服务在该共享里准备 CAD 工作文件。
- 图纸编辑依赖 Windows 上的 CAXA：系统通过编辑会话在 SMB 工作目录中打开 EXB/DWG，用户在自己的 CAXA 里改完回存，系统再读回。EXB 转换走本机转换器链路。
- 主要图纸格式：EXB、DWG、DXF、PDF；另有三维模型、零件文件与 BOM。
- 上传附件落盘规则：`CAD_STORAGE_ROOT/<图号>/<原始文件名>`，单文件上限 `CAD_MAX_UPLOAD_MB=100`。
- 车间、生产、管理岗都使用办公室电脑查看图纸，本期不要求移动端或平板可用性（本次确认）。
- 图纸库检索是最高频入口，用户按**原图号**查图，不需要输入计划号、批次或启动时间。
- 系统**不管理**生产排程：启动日期、加工安排、生产计划负责人、生产进度、生产批次都不是本系统数据对象。外部计划号只作为可选来源编号／来源备注。
- 周启动表只是外部来源参考，且只有粘贴文本（原 `.xlsx` 下载到的是错误网页，未成功作为工作簿读取），不能作为图号台账或导入规则。

## Capabilities and Constraints

**已实现（代码为准）**

- 图纸库与零件库；图纸创建向导（总图 + 零件 + 三维模型 + 资料上传、转换状态与逐文件重试）；图纸对比；图纸详情 12 个子入口（预览、结构、模型、版本、借用、材料、工艺、审核、属性、文件、生命周期、变更）。
- 审核中心：待办、审核工作区、已完成；多轮退回与重提；审核意见按账号、案例、轮次、节点保存。
- 变更工单：申请、批准、执行、验收，佐证资料随单上传，变更后可在审核中心重新发起完整审核。
- 零件视图一期：从 CAD 标题栏生成检索索引（不是 BOM 数量清单，也不修改 CAD 原件），人工修订保存在索引层。
- 专利台账：专利主档、缴费按周期自动推算、到期提醒与凭证上传。
- 通知中心（服务端待办 + 桌面通知）、操作日志与系统日志、后台管理（账号、审核流、图纸、变更审批、变更记录、属性、转换队列、通知、客户端更新）。
- 架构：前端只有服务端 API，旧 `/api/data/*` 整表写入与旧 DataManager 已删除（Phase 0–9）。

**已冻结的领域规则（改动需重新评审）**

- Part 是全局唯一的稳定工程零件身份；PartRevision 是零件的工程内容版本。
- Borrowed Part 动态读取源 Part 最新的 PublishedRevision；V1 不支持借用完整装配子树；借用件不可直接修改，必须 Fork。Fork 采用旧借用关系归档 + 新建自有关系并保留 lineage。
- Draft 可编辑并使用 `row_revision` CAS；Reviewing 禁止普通编辑，只能通过审核动作；Published 永久不可修改。
- DrawingPartRelation 是图纸中的使用实例，关系自身带 `revision`；`qty`／`parent`／`position`／`remark` 属于关系而非零件。
- 结构禁止成环；同一 Drawing 的结构写入使用 drawing-level lock。
- 附件按 Attachment／AttachmentVersion／Blob 分层，`part_revision_attachments` 只引用 `attachment_version_id`；正式引用的版本必须受保护。
- 旧 Tauri 客户端与新后端不兼容，前后端必须一起升级。

**边界与约束**

- 业务模型为两层：**项目 → 原图号**。计划层已取消，计划号不是 CAD 主对象（本次确认）。
- 编号与身份：项目号、图号、变更号、发放号、专利号分别管理，互不替代；内部关系用稳定 UUID，编号更正保留别名；只规范首尾空格等无意义差异，原始编号始终保留。
- 老图导入保留原图号、原文件名、原版本、原目录和来源记录；无法确定项目归属时进入"待归属"，不编造计划号或新图号。
- 权限：登录后共享读取是当前策略，**尚未实现按项目成员隔离**；受限资料关联进公开项目不得扩大可见范围。
- 未决：角色是否扩展为方案第 12 节的多职责分工；专利检测（侵权候选）是否落地；移动端／平板扩展；增强项（内容检索与 OCR、高级对比、外部协同、流程模板）。
- 已交付范围以本机自动化回归为准；真实数据库、对象存储、SMB／CAXA 与浏览器业务 E2E 需现场验证，此前不宣称业务 E2E 完成。

## Brand Commitments

- 产品在应用内的正式名称是**「图枢 · CAD 图纸管理系统」**（见 `vue/cad/index.html` 的 `<title>`）；仓库与工程名为 `cadguanliq`。界面语言为中文（`lang="zh-CN"`），业务术语沿用既有用词：图纸、原图号、项目、变更、发布包、发放、零件、借用、分叉继承、待归属。
- 未建立正式品牌体系：没有确定的 Logo、企业名和对外视觉资产。根目录 `pelican.svg` 是一张与产品无关的鹈鹕插图，不作为品牌素材。
- 方案明确的语气要求：关键限制用**原因 + 解决入口**解释（例如"原图号 V2 尚未完成重新审核，暂不能生成正式图纸发布包"）；必填项按阶段出现，登记时少填、批准／发布时补齐。

## Evidence on Hand

- `docs/project-plan-lifecycle/企业级项目计划图纸与资料全生命周期方案.md`（770 行，V1.2，2026-09-12）：产品方案主文档，含业务对象、资料分层、专利台账、变更闭环、生命周期、权限、档案、数据模型建议、分期交付与 37 条验收用例（A01–A37）。
- `docs/ui-ux-review-2026-09-14.md`：上线前 UI/UX 审查，含已修复的 7 项与 5 条信息组织建议。
- `docs/refactor-domain-architecture/`：Phase 0–9 设计冻结、数据库模型、Domain 与原子 API、前端 Modules、Pinia Read Model、页面迁移与旧架构清理记录；`当前任务.md` 是进度权威。
- `docs/part-index/`：零件视图一期实施契约、进度与验证记录。
- 代码本体即最真实证据：`go/internal/*`（含 drawing、partindex、change、review、attachment、versioning、editing、smb、converter、titleblock、notification、audit）与 `vue/cad/src/features/*`。
- **缺失且不得编造**：没有真实客户案例、证言、性能数据、定价或授权信息；没有登录后的浏览器截图与实际操作记录；没有可用的原始周启动表工作簿；没有生产数据库的核验快照。方案中出现的项目分组、图纸内容、变更故事与专利均为演示，不是已确认业务事实。

## Product Principles

1. **原图号不可改。** 任何新功能都不得为了适配外部编号而改号、复制图纸或制造虚假归属；不确定的归属进"待归属"。
2. **记录必须可信。** 文件内容写入不可覆盖的版本对象并登记 SHA-256；批准与快照以追加记录保存；正式审计记录应用账号无权修改。失败、退回、取消的业务记录不因未被最终采用而失去留存价值。
3. **一种状态不够。** 技术审核、项目引用、文件发送、对方回复、生产实施彼此独立，不得用单一状态字段合并表达；这些任一都不代表生产完成。
4. **一步一步交付，每步可运行。** 新增 → 验证 → 迁移调用方 → 验证 → 旧代码失去使用者 → 删除；不允许先删旧代码让项目整体不可运行，也不允许用假接口或吞异常通过验收。
5. **先给下一步，再给人看数据。** 用户任何时候都应知道"我在哪、刚发生了什么、下一步点哪里"；加载中、加载失败可重试、无匹配结果、确实没有数据是四种状态，不能合并。

## Accessibility & Inclusion

无正式无障碍标准，也不要求 WCAG 等级（本次确认）。产品自身的可用性底线来自使用现场：

- 字要大、要清晰，现场能看清、能点准（表格横向滚动、字级与底部按钮可见性须按业务常用窗口尺寸检查）。
- 常规动作自动形成记录，用户不用手写时间线；常见资料上传目标约 30 秒完成归类、凭编号约 10 秒定位正式图纸（待现场验证的目标，不是现有性能事实）。
- 权限不足与数据不存在按安全规则区分显示，不返回敏感项目名称。
